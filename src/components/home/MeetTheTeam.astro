---
const members = [
  {
    name: 'Joey McKenzie',
    title: 'Co-founder and Software Savant',
  },
  {
    name: 'Sarah McKenzie',
    title: 'Co-founder and Product Professional',
  },
  {
    name: 'Moose',
    title: 'Co-founder and Office Dog',
  },
];
---

<section id="meet-team-1370">
  <div class="cs-container">
    <div class="cs-content">
      <span class="cs-topper">Meet the Team</span>
      <h2 class="cs-title">Our Expert Team Members</h2>
      <p class="cs-text">
        At Doghead Digital, software is not only our profession, but our
        passion. We have a decade of experience building software for companies
        of all sizes, from startups getting off the ground, to global
        enterprises you've probably heard of. No matter the project, we're
        confident we've got the skill to bring your business to the next level.
      </p>
    </div>
    <ul class="cs-card-group">
      {
        members.map(({ name, title }) => (
          <li class="cs-item">
            <div class="cs-picture-wrapper">
              <picture class="cs-picture">
                <source
                  media="(max-width: 600px)"
                  srcset="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/MISC/agency-worker-1.png"
                />
                <source
                  media="(min-width: 601px)"
                  srcset="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/MISC/agency-worker-1.png"
                />
                <img
                  decoding="async"
                  src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/MISC/agency-worker-1.png"
                  alt="person"
                  width="413"
                  height="374"
                  aria-hidden="true"
                />
              </picture>
              <div class="cs-social">
                <a
                  href=""
                  class="cs-link"
                  aria-label="Twitter"
                  target="_blank"
                  rel="noopener"
                >
                  <img
                    class="cs-icon"
                    aria-hidden="true"
                    loading="lazy"
                    decoding="async"
                    src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Icons/facebook1a.svg"
                    alt="facebook"
                    width="15"
                    height="13"
                  />
                </a>
                <a
                  href=""
                  class="cs-link"
                  aria-label="Linked in"
                  target="_blank"
                  rel="noopener"
                >
                  <img
                    class="cs-icon"
                    aria-hidden="true"
                    loading="lazy"
                    decoding="async"
                    src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Icons/instagram1a.svg"
                    alt="instagram"
                    width="17"
                    height="16"
                  />
                </a>
                <a
                  href=""
                  class="cs-link"
                  aria-label="Twitter"
                  target="_blank"
                  rel="noopener"
                >
                  <img
                    class="cs-icon"
                    aria-hidden="true"
                    loading="lazy"
                    decoding="async"
                    src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Icons/twitter1a.svg"
                    alt="twitter"
                    width="15"
                    height="13"
                  />
                </a>
                <a
                  href=""
                  class="cs-link"
                  aria-label="Linked in"
                  target="_blank"
                  rel="noopener"
                >
                  <img
                    class="cs-icon"
                    aria-hidden="true"
                    loading="lazy"
                    decoding="async"
                    src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Icons/youtube1a.svg"
                    alt="youtube"
                    width="17"
                    height="16"
                  />
                </a>
              </div>
            </div>
            <div class="cs-info">
              <span class="cs-name">{name}</span>
              <span class="cs-job">{title}</span>
            </div>
          </li>
        ))
      }
    </ul>
  </div>
  <div class="cs-bubbles" aria-hidden="true"></div>
</section>

<style lang="scss">
  /* -- -------------------------- -->
<---        Recent Posts        -->
<--- -------------------------- - */

  /* Mobile - 360px */
  @media only screen and (width >= 0) {
    @keyframes floatAnimation {
      0% {
        transform: translateY(0);
      }

      50% {
        transform: translateY(-2rem);
      }

      100% {
        transform: translateY(0);
      }
    }

    @keyframes floatAnimation2 {
      0% {
        transform: translateY(0);
      }

      50% {
        transform: translateY(-3rem);
      }

      100% {
        transform: translateY(0);
      }
    }

    #meet-team-1370 {
      padding: var(--sectionPadding);
      position: relative;
      z-index: 1;
      overflow: hidden;

      .cs-container {
        width: 100%;
        max-width: calc(1280 / 16 * 1rem);
        margin: auto;
        display: flex;
        flex-direction: column;
        align-items: center;

        /* 48px - 64px */
        gap: clamp(3rem, 6vw, 4rem);
      }

      .cs-content {
        /* set text align to left if content needs to be left aligned */
        text-align: center;
        width: 100%;
        display: flex;
        flex-direction: column;

        /* centers content horizontally, set to flex-start to left align */
        align-items: center;
      }

      .cs-topper {
        font-size: var(--topperFontSize);
        font-weight: 700;
        line-height: 1.2em;
        text-align: inherit;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        margin-bottom: calc(4 / 16 * 1rem);
        color: var(--primary);
        display: block;
      }

      .cs-title {
        font-size: var(--headerFontSize);
        font-weight: 900;
        line-height: 1.2em;
        text-align: inherit;
        max-width: calc(700 / 16 * 1rem);
        margin: 0 0 calc(16 / 16 * 1rem);
        color: var(--headerColor);
        position: relative;
      }

      .cs-text {
        font-size: var(--bodyFontSize);
        line-height: 1.5em;
        text-align: inherit;
        width: 100%;
        max-width: calc(650 / 16 * 1rem);
        margin: 0;
        color: var(--bodyTextColor);
      }

      .cs-card-group {
        width: 100%;

        /* max-width is removed on tablet */
        max-width: calc(700 / 16 * 1rem);
        margin: 0;
        padding: 0;
        display: grid;
        align-items: stretch;
        grid-template-columns: repeat(12, 1fr);

        /* 16px - 20px */
        gap: clamp(1rem, 2vw, 1.25rem);
      }

      .cs-item {
        list-style: none;
        display: flex;
        flex-direction: column;
        grid-column: span 12;

        &:hover {
          .cs-link {
            opacity: 1;
            transform: translateY(0);
          }
        }
      }

      .cs-picture-wrapper {
        /* 12px - 20px */
        margin-bottom: clamp(0.75rem, 4vw, 1.25rem);
        position: relative;
      }

      .cs-picture {
        margin: 0 auto;
        border-radius: 50%;
        display: block;
        position: relative;
        z-index: 1;
        overflow: hidden;
        aspect-ratio: 1/1;

        img {
          width: 100%;
          height: 100%;
          object-fit: cover;
          position: absolute;
          top: 0;
          left: 0;
        }
      }

      .cs-info {
        text-align: center;

        /* 16px - 24px */
        padding: clamp(1rem, 1.7vw, 1.5rem);
        background-color: #f7f7f7;
        border-radius: calc(24 / 16 * 1rem);
        display: flex;
        flex-direction: column;
        justify-content: center;

        /* makes the info boxes grow when the items aren't matching heights */
        flex-grow: 1;
        transition: background-color 0.3s;

        &:hover {
          color: #fff;
          background-color: var(--primary);
        }
      }

      .cs-name {
        font-size: calc(20 / 16 * 1rem);
        font-weight: bold;
        margin: 0;
        margin: 0 0 calc(4 / 16 * 1rem);
        gap: calc(16 / 16 * 1rem);
        transition: color 0.3s;
      }

      .cs-job {
        transition: color 0.3s;
      }

      .cs-social {
        display: flex;
        gap: calc(8 / 16 * 1rem);
        position: absolute;
        right: 50%;
        bottom: calc(24 / 16 * 1rem);
        z-index: 1;
        transform: translateX(50%);
      }

      .cs-link {
        width: calc(40 / 16 * 1rem);
        height: calc(40 / 16 * 1rem);
        background-color: #fff;
        opacity: 0;
        border-radius: 50%;
        display: flex;
        justify-content: center;
        align-items: center;
        transform: translateY(1rem);
        transition:
          transform 0.3s,
          opacity 0.3s;

        &:hover {
          .cs-icon {
            /* use this pen to generate a filter from black to a color of your choosing */

            /* https://codepen.io/sosuke/pen/Pjoqqp */
            filter: invert(55%) sepia(42%) saturate(4845%) hue-rotate(337deg)
              brightness(101%) contrast(98%);
          }
        }

        &:first-of-type,
        &:last-of-type {
          margin-top: calc(-20 / 16 * 1rem);
        }

        &:nth-of-type(1) {
          transition-delay: 0.05s;
        }

        &:nth-of-type(2) {
          transition-delay: 0.1s;
        }

        &:nth-of-type(3) {
          transition-delay: 0.15s;
        }

        &:nth-of-type(4) {
          transition-delay: 0.2s;
        }
      }

      .cs-bubbles {
        width: calc(357 / 16 * 1rem);
        height: calc(470 / 16 * 1rem);
        margin-left: calc(-938 / 16 * 1rem);
        display: none;
        position: absolute;
        top: calc(-186 / 16 * 1rem);
        left: 50%;
        z-index: -1;

        &::before {
          /* white border bubble */
          content: '';
          width: calc(294 / 16 * 1rem);
          height: calc(294 / 16 * 1rem);
          background: transparent;
          opacity: 1;
          border: 1px solid #1a1a1a;
          border-radius: 50%;
          display: block;
          position: absolute;
          top: 0;
          right: 0;
          animation-name: floatAnimation;
          animation-duration: 5s;
          animation-timing-function: ease-in-out;
          animation-iteration-count: infinite;
          animation-fill-mode: forwards;
        }

        &::after {
          /* orange bubble */
          content: '';
          width: calc(294 / 16 * 1rem);
          height: calc(294 / 16 * 1rem);
          background: var(--primary);
          opacity: 0.1;
          border-radius: 50%;
          display: block;
          position: absolute;
          bottom: 0;
          left: 0;
          z-index: -1;
          animation-name: floatAnimation2;
          animation-duration: 14s;
          animation-timing-function: ease-in-out;
          animation-iteration-count: infinite;
          animation-fill-mode: forwards;
        }
      }
    }
  }

  /* Tablet - 500px */
  @media only screen and (width >= 31.25rem) {
    #meet-team-1370 {
      .cs-item {
        grid-column: span 4;
      }
    }
  }

  /* Desktop - 1024px */
  @media only screen and (width >= 64rem) {
    #meet-team-1370 {
      .cs-container {
        flex-direction: row;
      }

      .cs-content {
        text-align: left;
        max-width: calc(413 / 16 * 1rem);
        align-items: start;
      }

      .cs-card-group {
        max-width: none;
      }

      .cs-bubbles {
        display: block;
      }
    }
  }

  /* Dark Mode */
  @media only screen and (width >= 0) {
    body.dark-mode {
      #meet-team-1370 {
        .cs-topper {
          color: var(--primaryLight);
        }

        .cs-title,
        .cs-text {
          color: var(--bodyTextColorWhite);
        }

        .cs-text,
        .cs-job {
          opacity: 0.8;
        }

        .cs-info {
          background-color: var(--medium);
        }
      }
    }
  }
</style>
